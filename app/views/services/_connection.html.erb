<tr>
<td><% if connection.arrow_down? %>&darr;<% elsif connection.arrow_up? %>&uarr;<% end %><%= connection.identifier %></td><td><%= link_to image_tag("edit.png", height: '16', width: '16'), edit_connection_path(connection) %></td>
</tr>
<%= render partial: "component", layout: "component", object: connection.next_component unless connection.next_component.nil? %>
