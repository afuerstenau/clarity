<tr>
<td><% if connection.arrow_down? %>&darr;<% elsif connection.arrow_up? %>&uarr;<% end %>
  <% if connection.identifier_link.blank? %>
  <%= connection.identifier %>
  <% else %>
  <%= link_to connection.identifier, connection.identifier_link %>
  <% end %>

</td><td><%= link_to image_tag("edit.png", height: '16', width: '16'), edit_connection_path(connection) %></td>
</tr>
<%= render partial: "component", object: connection.next_component unless connection.next_component.nil? %>
<% if connection.next_component.nil? then %>
<tr>
  <td><%= link_to 'New Component', new_component_path(previous_connection_id: connection) %></td>
</tr>
<% end %>
