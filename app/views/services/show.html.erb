<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @service.name %>
</p>

<table style="width=100%">
<%= render partial: "connection", layout: "connection", object: @service.first_connection  %>
</table>
<%= link_to 'Edit', edit_service_path(@service) %> |
<%= link_to 'Back', services_path %>
