<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @service.name %>
</p>
  <table id="connection">
    <td style="width:50%"></td>
    <td>&darr;<%= @service.first_connection.identifier unless @service.first_connection.nil? %></td>
  </table>



<%= link_to 'Edit', edit_service_path(@service) %> |
<%= link_to 'Back', services_path %>
