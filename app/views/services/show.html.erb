<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @service.name %>
</p>

<table style="width=100%">
<%= render partial: "connection", object: @service.first_connection  unless @service.first_connection.nil?%>
</table>

Monitoring:
<p>
  <%= render @service.monitoring unless @service.monitoring.nil? %>
  <%= link_to 'New Monitoring', new_monitoring_path(service: @service) if @service.monitoring.nil? %>
</p>
<%= link_to 'Edit', edit_service_path(@service) %> |
<%= link_to 'Back', services_path %>
